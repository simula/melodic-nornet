#!/usr/bin/env bash

# Bash options:
set -eu

${MAKE_GLOSSYTEXT} "${OUTPUT}"   \
   "MELODIC"   1024              \
   --bordersize 30               \
   --colorscheme "Golden"        \
   --font "DejaVu Sans Bold"
